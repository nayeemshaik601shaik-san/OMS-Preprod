name: OMS Preprod Pipeline
on:
    push:
      branches:
        - main
    workflow_dispatch:
jobs:
    build:
      uses: nayeemshaik601shaik-san/OMS-Workflows/.github/workflows/oms-build.yml@main
      with:
        environment: preprod
        projectdir: ${{ github.workspace }}/sourcecode
